fastapi==0.115.6
uvicorn[standard]==0.34.0
pymongo[srv]>=4.6.0
textblob==0.15.3
python-dotenv==1.0.1
Werkzeug==2.3.7
python-jose[cryptography]==3.3.0
email-validator==2.2.0

# ----------------- OPTIONAL PACKAGES -----------------
# The following packages require a C/C++ compiler to compile on Python 3.14 (pre-release).
# We have made them optional in the backend code so you can run the app without compiling them.
# If you have MSVC/VS Build Tools installed, you can uncomment them to enable LIME visualizations:
# lime==0.2.0.1
# scikit-learn==1.6.0